{
  "name": "better-photos",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "compile": "sass --no-source-map sass/main.scss css/main.css -w"
  },
  "devDependencies": {
    "vite": "^3.0.0"
  },
  "dependencies": {
    "sass": "^1.53.0"
  }
}
